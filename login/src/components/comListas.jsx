import React from "react";

import { useAuth } from "@/hooks";

export default function comListas() {
  /* const { user } = useAuth();
  console.log(useAuth()); 
  solo user ahora*/
  return <div>comListas</div>;
}

//es un ejemplo de usar el hook
